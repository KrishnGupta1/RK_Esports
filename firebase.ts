// Firebase has been temporarily disabled as requested.
// Mock exports are provided to prevent import errors in files that haven't been fully migrated yet.

export const auth = {};
export const db = {};
export const analytics = null;
